# Python dependencies for the CIERRE-TEKTRON maintenance / diagnostic scripts.
#
# NOTE: CPU-only PyTorch (a transitive dependency of sentence-transformers) is
# installed separately from the official PyTorch CPU wheel index in
# .cursor/install.sh so that the CUDA build is not pulled in. Install that
# first, or run `.cursor/install.sh`, before installing this file.
numpy==2.5.2
faiss-cpu==1.15.0
sentence-transformers==6.0.0
rank-bm25==0.2.2
requests==2.34.2
